<!doctype html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

    <title>{{portlet}} - {{title}}</title>
    <link rel="shortcut icon" href="/@mobile/favicon.ico" type="image/x-icon">
    <script src="/@mobile/vue@2.6.11.min.js"></script>
    <script src="/@mobile/axios@0.19.2.min.js"></script>
    <link rel="stylesheet" type="text/css" href="/@mobile/tailwind.base@1.2.0.min.css">
    <style>
        body {
            margin: 0;
        }

        [v-cloak] {
            display: none !important;
        }

        .button_border_green {
            background-color: #53CD9B;
            color: #FDFFFF;
            box-shadow: 0px 5px 10px 0px #53CD9B;
            border: 1px solid #53CD9B;
        }

        .top_fix {
            border-top: 1px soild #e4e4e4;
            position: fixed;
            top: 0px;
            background-color: #fff;
            width: 100%;
            display: flex;
        }

        .item_border_white {
            box-shadow: 0px 3px 10px 0px rgba(231, 231, 231, 1);
            border: 1px solid rgba(247, 247, 247, 1);
        }
    </style>
    {{{twBlock}}}
    {{{block 'styles'}}}
    {{{block 'scripts@head'}}}
    <script type="text/javascript">
        $mixins = []
        function toQuery(params) {
            if (!params) return ''
            var parts = []
            Object.entries(params).forEach(function ([key, val], index) {
                parts.push(key + '=' + encodeURIComponent(val))
            })
            return parts.join('&')
        }
        function toFullUrl(url, params) {
            return url + (url.indexOf('?') == -1 ? '?' : '') + toQuery(params)
        }
        function goto(url) {
            location.href = url
        }
    </script>
</head>

<body>
    <div id='app'  v-cloak>
        {{{body}}}
    </div>
    {{{block 'scripts'}}}
    <script type="text/javascript">
        new Vue({
            mixins: $mixins,
            el: '#app'
        })
    </script>
</body>

</html>