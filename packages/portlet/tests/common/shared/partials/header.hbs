
<span>Vimesh Portlet Test</span>
    <el-dropdown style="float: right;">
        <span class="el-dropdown-link">
            {{_user.name}}<i class="el-icon-arrow-down el-icon--right"></i>
        </span>
        <el-dropdown-menu slot="dropdown">
            <el-dropdown-item v-for="item in avatarMenus.menus" @click="onAvatarMenuClick(item)">\{{item.title}}
            </el-dropdown-item>
        </el-dropdown-menu>
    </el-dropdown>
<style>
    .el-dropdown-link {
        cursor: pointer;
        color: #409EFF;
    }

    .el-icon-arrow-down {
        font-size: 12px;
    }
</style>

{{#contentFor 'scripts'}}

<script type="application/javascript">
    var $avatarMenus = {{{ menusByZone 'avatar' }}}
    $mixins.push({
        data: function () {
            return {
                avatarMenus: $avatarMenus
            }
        },
        methods: {
            onAvatarMenuClick(item) {
                console.log(item)
            }
        }
    })
</script>
{{/contentFor}}